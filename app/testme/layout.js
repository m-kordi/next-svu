export const metadata = {
    title: 'hallo',
    description: 'boooooo',
  }
  
  export default function Layout({ children }) {
    return (
      <div>
        {/* your layout component */}
        {children}
      </div>
    );
  }

  